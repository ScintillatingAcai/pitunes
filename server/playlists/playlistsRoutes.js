var playlistsController = require('./playlistsController.js');
var utility = require('../utility');

module.exports = function(app) {

  //playlist routes all go through the user route for session checks
};

